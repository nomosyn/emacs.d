pick 3a774f4 replace symbolic link "Typechange stories/class-template.js" by its value
pick f08fe96 Update stories/problem-template-draft.org
pick 7e42a58 problem-template-draft.org -> problem-template.org
pick 4e6d02e update coding style
pick a7455ae fixup

# Rebase e912e65..a7455ae onto e912e65 (5 command(s))
#
# Commands:
# p, pick = use commit
# r, reword = use commit, but edit the commit message
# e, edit = use commit, but stop for amending
# s, squash = use commit, but meld into previous commit
# f, fixup = like "squash", but discard this commit's log message
# x, exec = run command (the rest of the line) using shell
#
# These lines can be re-ordered; they are executed from top to bottom.
#
# If you remove a line here THAT COMMIT WILL BE LOST.
#
# However, if you remove everything, the rebase will be aborted.
#
# Note that empty commits are commented out
